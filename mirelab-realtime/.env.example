# 이 파일을 .env 로 복사해서 채운다 (gitignore 됨). npm run dev 가 자동으로 읽는다.

# 백엔드 application-secret.properties 의 mirelab.internal-secret 과 같은 값.
# 다르면 스냅샷 저장·접속 인가가 전부 403 이 되고, 이 값이 아예 없으면 서버가 뜨지 않는다.
INTERNAL_SECRET=

SPRING_BASE_URL=http://localhost:8080
